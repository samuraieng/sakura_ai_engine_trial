"""
ダイクストラ迷路可視化デモの共通設定。
"""

# ---------------------------------------------------------------------------
# 迷路の基本設定
# ---------------------------------------------------------------------------
GRID_SIZE = 100               # 迷路のマス数（100 x 100）
WALL_THICKNESS = 5            # 壁の太さ [px]
PATH_THICKNESS = 1            # 通路の太さ [px]
CELL_UNIT = WALL_THICKNESS + PATH_THICKNESS  # 1マスあたりの論理ピクセル数 (6)

# ウィンドウ/録画サイズ
# 100マス + 外周壁を含めると 100*6 + 5 = 605px となる
LOGICAL_SIZE = GRID_SIZE * CELL_UNIT + WALL_THICKNESS
WINDOW_SIZE = (LOGICAL_SIZE, LOGICAL_SIZE)
GIF_SIZE = (320, 320)         # 録画 GIF は 320x320 以下にする

# ---------------------------------------------------------------------------
# 出発点・到達点
# 行0が画面上部なので、左下=(0, 99)、右上=(99, 0)
# ---------------------------------------------------------------------------
START = (0, GRID_SIZE - 1)
GOAL = (GRID_SIZE - 1, 0)

# ---------------------------------------------------------------------------
# 色定義 (R, G, B)
# ---------------------------------------------------------------------------
COLOR_UNUSED = (255, 255, 255)   # 白：未利用
COLOR_VISITED = (255, 0, 0)      # 赤：通過（候補）
COLOR_ADOPTED = (0, 0, 255)      # 青：通過＆採用
COLOR_WALL = (0, 0, 0)           # 黒：壁
COLOR_TEXT = (0, 0, 0)           # 黒：テキスト
COLOR_PROGRESS_BG = (200, 200, 200)
COLOR_PROGRESS_FG = (0, 128, 0)

# ---------------------------------------------------------------------------
# アニメーション・録画設定
# ---------------------------------------------------------------------------
WINDOW_FPS = 60
UPDATE_EVERY_N_STEPS = 10     # 1フレームあたりのダイクストラ更新回数
CAPTURE_EVERY_N_FRAMES = 4    # 何フレームに1回録画するか（15fps相当）
RECORDER_FPS = 15

# 乱数シード（同じシードなら同じ迷路が生成される）
SEED = 42
